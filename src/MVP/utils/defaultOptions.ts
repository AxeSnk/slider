import { IOptions } from './IOptions';

const defaultOptions: IOptions = {
  val: 4,
  valEnd: 6,
  minVal: 0,
  maxVal: 10,
  step: 1,
  range: false,
  tooltip: true,
  vertical: false,
  scale: true,
};

export default defaultOptions;
